import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#C49331",
    },
    secondary: {
      main: "#FF5D00",
    },
    error: {
      main: "#917D55",
    },
  },
});

export default theme;
